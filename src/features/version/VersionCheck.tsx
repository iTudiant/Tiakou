import { MainScreen, Row, Text } from "_shared";

const VersionCheck = () => {
  return (
    <Row marginVertical="s">
      <Text variant={"tertiary"} color="text">
        Version 0.0.1
      </Text>
    </Row>
  );
};

export default VersionCheck;
